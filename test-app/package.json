{
  "name": "stats-charts-test-app",
  "version": "1.0.1",
  "description": "Test-app for testing the @anjson/stats-charts library.",
  "type": "module",
  "browser": "src/js/index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "lint": "npx eslint ./src || exit 0",
    "lint:fix": "npx eslint ./src --fix || exit 0"
  },
  "contributors": [
    "Anders Jonsson <aj224rj@student.lnu.se>"
  ],
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@lnu/eslint-config": "^1.1.4",
    "vite": "^3.1.8"
  },
  "dependencies": {
    "@anjson/stats-charts": "^2.0.1"
  }
}
